interface:
  display_name: "Trading Tools"
  short_description: "View Binance futures positions and charts."
  default_prompt: "Use $trading-tools to render an NVDAUSDT futures chart."
policy:
  allow_implicit_invocation: true
